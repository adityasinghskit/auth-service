spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5432/test'
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        default_schema: auth_service
        cache:
          use_second_level_cache: false